#include "main.h"

/**
 * mul - multiplay two.
 * @a: input number.
 * @b: input number.
 * Return: the result of the mul always success.
 */
int mul(int a, int b)
{
	int result = a * b;

	return (result);
}
