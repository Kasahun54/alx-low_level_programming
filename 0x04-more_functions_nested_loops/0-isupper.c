#include "main.h"

/**
 * _isupper - check for upper case latter
 *
 * return 1 if the letter is capital else 0
 */
int _isupper(int c)
{
	if (c >= 65 && c <= 90)
	{
		return (1);
	else 
	{
		return (0);
	}
}
